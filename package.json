{
  "name": "greenmart-site",
  "private": true,
  "type": "module",
  "description": "Green Mart static site plus Netlify Functions for shop data",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.1"
  }
}
